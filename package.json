{
  "name": "examination-flow-demo",
  "version": "0.1.0",
  "private": true,
  "description": "MVP demo for a Canadian patent examination workflow using Perplexity Sonar.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
